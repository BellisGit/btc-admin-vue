{
  "style": { "maxFileLines": 300, "maxFunctionLines": 50 },
  "architecture": {
    "forbidPatterns": [
      { 
        "pattern": "src/modules/**/pages/**/*.vue", 
        "forbid": ["axios","fetch"], 
        "message": "I/O 必须走 btc/service 层" 
      },
      { 
        "pattern": "src/plugins/**/components/**/*.vue", 
        "forbid": ["defineComponent\\({\\s*data\\s*\\("], 
        "message": "使用 <script setup> + composables" 
      }
    ],
    "enforceLayering": [
      { 
        "from": "src/modules/**", 
        "allow": ["/@/**","/#/**","btc/**"] 
      }
    ]
  },
  "review": {
    "requireTestsFor": ["src/btc/**","src/plugins/**"],
    "requireStoriesFor": ["src/components/**"]
  }
}